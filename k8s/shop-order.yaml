# shop-order on Kubernetes (preprod).
# Apply: kubectl --context kind-preprod apply -f shop-order/k8s/shop-order.yaml
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: shop-order-config
  namespace: shop
data:
  SPRING_PROFILES_ACTIVE: "preprod"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shop-order
  namespace: shop
  labels:
    app: shop-order
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shop-order
  template:
    metadata:
      labels:
        app: shop-order
    spec:
      containers:
        - name: shop-order
          image: localhost/shop-order:0.0.1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: shop-order-config
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              memory: "512Mi"
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            periodSeconds: 5
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: shop-order
  namespace: shop
spec:
  type: ClusterIP
  selector:
    app: shop-order
  ports:
    - name: http
      port: 8080
      targetPort: 8080
